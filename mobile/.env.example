# Expo public env vars (auto-exposed to the app bundle)
# Replace with your Paystack Public Key (test or live)
EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxx

# Usage: app/_layout.tsx reads process.env.EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY
# After copying this file to .env and setting the key, restart the Expo dev server.
